export default class User {
    constructor(username, email, password, surname, name, secondname) {
        this.username = username;
        this.email = email;
        this.password = password;
        this.surname = surname;
        this.name = name;
        this.secondname = secondname;
    }
}