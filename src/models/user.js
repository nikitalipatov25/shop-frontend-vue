export default class User {
    constructor(username, email, password, surname, name) {
        this.username = username;
        this.password = password;
        this.email = email;
        this.surname = surname;
        this.name = name;
    }
}